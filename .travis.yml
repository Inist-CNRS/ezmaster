sudo: required
language: node_js
node_js:
  - 4.4.0
services:
  - docker
before_install:
  - sudo apt-get update
  - sudo apt-get install -o Dpkg::Options::="--force-confold" --force-yes -y docker-engine
  - make install
  - make build
  - make run-prod
script:
  - make test